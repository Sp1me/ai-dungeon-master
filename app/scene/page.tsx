import { SceneAtlasClient } from "@/components/game/scene-atlas-client";

export default function ScenePage() {
  return (
    <main className="page-shell">
      <SceneAtlasClient />
    </main>
  );
}
